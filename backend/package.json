{
  "scripts": {
    "dev": "npm run dev --prefix dizitup-backend",
    "start": "npm start --prefix dizitup-backend",
    "build": "npm install && npm install --prefix dizitup-backend && npx prisma db push && npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^7.4.2",
    "bcryptjs": "^3.0.3"
  },
  "devDependencies": {
    "@prisma/config": "^7.6.0",
    "dotenv": "^17.3.1",
    "prisma": "^7.4.2"
  }
}
